#!/usr/bin/python3
if __name__ != "__main__":
    exit()
    from sys import argv
    argc = len(argv) - 1
    i = 0
    res = 0
    for arg in argv:
        if != 0:
            res += int(arg)
        else:
            i += 1
        print("{:d}".format(res))
